认证相关：
  /auth/login
  /auth/register
  /auth/logout

文件操作：
  /file/upload
  /file/download  
  /file/delete
  /file/rename
  /file/move
  /file/copy

目录操作：
  /directory/create
  /directory/delete
  /directory/list
  /directory/rename

传输控制：
  /transfer/start      # 开始传输会话
  /transfer/window     # 请求传输窗口
  /transfer/ack        # 确认接收
  /transfer/end        # 结束传输

查询搜索：
  /search/files
  /browse/path





读 -> 处理报文 -> 通知写 -> epoll收到 -> 将写任务传入任务队列
