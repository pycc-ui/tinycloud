登录成功后的流程

1.服务端向客户端发送个文件目录的结构

2.客户端取出这个目录结构

3.客户端将这个结构展示出来

4.客户端有POST请求,POST请求只负责增删改

5.客户端有GET请求,GET请求只负责上传文件

6.传送不指定文件的类型,只负责传送文件和文件名到指定目录

7.要考虑大文件的传输

8.用户的POST请求会导致目录结构的变化,会向客户端重新发送目录文件
或者POST请求报文附带目录结构,直接在本地改变目录结构
这涉及到本地和云端的同步

认证相关：
  /auth/login
  /auth/register
  /auth/logout

文件操作：
  /file/upload
  /file/download  
  /file/delete
  /file/rename
  /file/move
  /file/copy

目录操作：
  /directory/create
  /directory/delete
  /directory/list
  /directory/rename

传输控制：
  /transfer/start      # 开始传输会话
  /transfer/window     # 请求传输窗口
  /transfer/ack        # 确认接收
  /transfer/end        # 结束传输

查询搜索：
  /search/files
  /browse/path

